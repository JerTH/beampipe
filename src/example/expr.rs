
{
    fn main() {
        10 + 10
    }

    fn foo(qq: i32) {
        local_1 = 3.3 + qq;
        bar = 10.0;
        g = 9.0 + 4.0 * local_1 + 2.0 / 3.0 - 99.0 * bar;
        g * g;
    }

    fn bar(a: i32, b: i32) {
        a * a + b * b
    }
}
